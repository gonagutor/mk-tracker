export type Driver = {
  id: string;
  name: string;
  icon: string;
  full_image: string;
};

export type Wing = {
  id: string;
  name: string;
  icon: string;
};

export type Kart = {
  id: string;
  name: string;
  icon: string;
};

export type Wheel = {
  id: string;
  name: string;
  icon: string;
};
